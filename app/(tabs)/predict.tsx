// app/(tabs)/history.tsx
import { Text, View } from "react-native";

export default function Predict() {
  return (
    <View style={{ flex: 1, justifyContent: "center", alignItems: "center" }}>
      <Text>Predict Page</Text>
    </View>
  );
}
